# Firmware UI Adapter

This directory contains firmware-facing LVGL code. Keep generated LVGL Editor
runtime files in the Editor-managed project files listed by `file_list_gen.cmake`;
application flow and hardware-facing UI updates belong here.
